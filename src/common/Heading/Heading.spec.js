// import React from 'react';
// import { render, fireEvent } from 'react-testing-library';
// import App from './App';

// test('App works', () => {
//   const { container } = render(<App />);
//   const buttons = container.querySelectorAll('button');

//   const result = container.querySelector('span');
//   expect(result.textContent).toBe('0');
// });

// import * as heading from './Heading';

// describe('Mock return value', () => {
//   it('Should mock the return value of sum', () => {
//     render(<App />);

//     const foundHeading = screen.getElementsByClassName("Heading")[1];
//     // Get the dog food buy button from an array of all buy butto
//     expect(foundHeading.toEqual('primary');
//   });
// });
